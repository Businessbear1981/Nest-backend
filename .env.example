# ------------ wired + used by the current codebase ------------
PORT=6000
HOST=0.0.0.0
DEBUG=false
SECRET_KEY=nest-dev-secret-change-me
FRONTEND_ORIGIN=http://localhost:4000
FUND_TICK_SECONDS=60
B_TRANCHE_COUPON_PCT=0.085
MGMT_FEE_PCT=0.02
WC_SPREAD_BPS=275
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-6
ANTHROPIC_MAX_TOKENS=4096

JWT_SECRET_KEY=nest-dev-jwt-change-me
JWT_TTL_HOURS=24

# ------------ NOT wired yet — placeholders for future series ------------
# Supabase (auth currently uses in-memory store; swap later)
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
# Market data (Vector agent — not built)
FRED_API_KEY=
# Outbound email (Aria send flow — not built; right now Aria only drafts)
SENDGRID_API_KEY=
# AEC token subscription payments (not built)
STRIPE_SECRET_KEY=
